# AppSync source: 3pCups

Public AppSync source for the 3P Cups apps.

Add it in the AppSync client with the source name `3pcups` (case-insensitive —
`3pCups` works too). The Hub resolves it to this repo
(`unix14/appsync_3pcups_source`) and merges `source.json` into the device catalog.
